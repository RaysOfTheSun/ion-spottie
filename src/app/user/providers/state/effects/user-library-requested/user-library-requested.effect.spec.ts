import { UserLibraryRequestedEffect } from './user-library-requested.effect';

describe('UserLibraryRequested.Effect', () => {
  it('should create an instance', () => {
    expect(new UserLibraryRequestedEffect()).toBeTruthy();
  });
});
